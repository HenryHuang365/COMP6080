import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fe3a5b', // Your custom color
    },
    // Additional theme customizations
  },
});

export default theme;
